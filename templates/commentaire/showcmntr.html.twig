{% extends 'back.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCom</th>
                <td>{{ commentaire.idCom }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index_back') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit_back', {'idCom': commentaire.idCom}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
