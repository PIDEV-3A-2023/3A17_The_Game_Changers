{% extends 'back.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <div class="card shadow-lg border-0 rounded-lg mt-5">
                <div class="card-header"><h3 class="text-center font-weight-light my-4">Add SuiviReclamation</h3></div>
                <div class="card-body">
                    {{ include('suivi_reclamation/_form.html.twig') }}
                    <div class="mt-4 mb-0">
                        <a href="{{ path('app_suivi_reclamation_index') }}" class="btn btn-primary btn-block">back to list</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3"></div>
    </div>
{% endblock %}
