{% extends 'back.html.twig' %}

{% block title %}SuiviReclamation{% endblock %}

{% block body %}
    <h1>SuiviReclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suivi_reclamation.id }}</td>
            </tr>
            
            <tr>
                <th>ReponseReclamation</th>
                <td>{{ suivi_reclamation.reponseReclamation }}</td>
            </tr>
             <tr>
                <th>contenuReclamation</th>
                <td>{{ suivi_reclamation.idReclamation.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_suivi_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_suivi_reclamation_edit', {'id': suivi_reclamation.id}) }}">edit</a>

    {{ include('suivi_reclamation/_delete_form.html.twig') }}
{% endblock %}
