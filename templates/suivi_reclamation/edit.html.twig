{% extends 'back.html.twig' %}

{% block title %}Edit SuiviReclamation{% endblock %}

{% block body %}
    <div id="page-content">
        <!--Page Title-->
        <div class="page section-header text-center">
            <div class="page-title">
                <div class="wrapper"><h1 class="page-width">Edit SuiviReclamation</h1></div>
            </div>
        </div>
        <!--End Page Title-->

        <div class="card">
            <div class="card-body">
                <div class="form-container">
                    {{ include('suivi_reclamation/_form.html.twig', {'button_label': 'Update'}) }}
                </div>

                <div class="text-center">
                    <a href="{{ path('app_suivi_reclamation_index') }}">Back to list</a>
                </div>

              
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('path/to/style.css') }}">
{% endblock %}
